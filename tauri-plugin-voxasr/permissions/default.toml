"$schema" = "schemas/schema.json"

[default]
description = "Allows VoxTerm to start/stop on-device transcription, poll its transcript, and delete a session's audio."
permissions = ["allow-start-transcribe", "allow-stop-transcribe", "allow-poll-transcript", "allow-delete-audio"]
