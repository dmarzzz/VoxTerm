{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "VoxTerm mobile: on-device transcription (voxasr) by default, with a LAN-pairing fallback to a desktop engine. No shell, no sidecar.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": ["core:window:default", "core:webview:default", "core:event:default", "voxasr:default", "voxllm:default"]
}
