"$schema" = "schemas/schema.json"

[default]
description = "Allows VoxTerm to run on-device LLM text generation and poll its result."
permissions = ["allow-llm-available", "allow-start-generate", "allow-poll-generate", "allow-cancel-generate"]
